# Providing tool functions to handle the xyz structure

def move_one_along(xyz, number, along, disp):
    """
    Description:
        move one atom with number along some direction by disp
    xyz:
        an instance of base.xyz.base_xyz
    number:
        the i-th atom in the xyz.atoms, starting from 0
    along:
        a list with three element, specifying the moving direction
        in catesian coordinates system
    disp:
        displacement in unit of Angstrom
    """
    pass
